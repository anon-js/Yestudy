import {HeaderText, MainView} from '../styles/index.styled.ts';

const MyScreen = () => {
  return (
    <MainView>
      <HeaderText>
        내 정보
      </HeaderText>
    </MainView>
  );
};

export default MyScreen;
